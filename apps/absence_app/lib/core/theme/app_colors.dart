import 'package:flutter/material.dart';

/// Cores da aplicação Absence - Padrão Minimalista (apenas cinza/branco)
class AppColors {
  // Tema Escuro
  static const Color darkBackground = Color(0xFF1D1D22);
  static const Color darkSurface = Color(0xFF24232B);
  static const Color darkOnSurface = Color(0xFFE8E8E8);  // Branco suave para melhor contraste

  // Tema Claro
  static const Color lightBackground = Color(0xFFFFFFFF);
  static const Color lightSurface = Color(0xFFF5F5F5);
  static const Color lightOnSurface = Color(0xFF1D1D22);  // Cinza bem escuro para contraste

  // Cores principais (neutras - padrão minimalista)
  static const Color primary = Color(0xFFFFFFFF);         // Branco para destaque
  static const Color secondary = Color(0xFF6B6B6B);       // Cinza médio
}
